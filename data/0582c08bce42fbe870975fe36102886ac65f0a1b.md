# Test info

- Name: Validate accessibility widget checkboxes >> Verify Bigger Text toggle's expected behaviour
- Location: /__w/KMSlh/KMSlh/tests/accessibility.test.ts:16:9

# Error details

```
Error: browserType.launch: Target page, context or browser has been closed
Browser logs:

╔═══════════════════════════════════════════════════════════════════════════════════════════════════════════════════╗
║ Firefox is unable to launch if the $HOME folder isn't owned by the current user.                                  ║
║ Workaround: Set the HOME=/root environment variable in your GitHub Actions workflow file when running Playwright. ║
╚═══════════════════════════════════════════════════════════════════════════════════════════════════════════════════╝
Call log:
  - <launching> /ms-playwright/firefox-1482/firefox/firefox -no-remote -headless -profile /tmp/playwright_firefoxdev_profile-5fyiyb -juggler-pipe -silent
  - <launched> pid=900
  - [pid=900][err] Running Nightly as root in a regular user's session is not supported.  ($HOME is /github/home which is owned by pwuser.)
  - [pid=900] <process did exit: exitCode=1, signal=null>
  - [pid=900] starting temporary directories cleanup

```

# Test source

```ts
   1 | import { test, expect } from '../fixtures/baseFixture.js';
   2 |
   3 | test.describe("Validate accessibility widget checkboxes", () => {
   4 |
   5 |     test("verify accessibility widget opens", async ({page, accessibility}, testInfo) => {
   6 |         console.log(`Running on ${testInfo.project.name}`);
   7 |         await accessibility.OpenAccessibilityWidget();
   8 |         await expect(await accessibility.isAccessibilityMenuOpen()).toBe(true);
   9 |     })
  10 |
  11 |     test("Validate Default State of Accessibility Toggles", async ({page, accessibility}) => {
  12 |         await accessibility.OpenAccessibilityWidget();
  13 |         await accessibility.checkDefaultToggleState();
  14 |     })
  15 |
> 16 |     test("Verify Bigger Text toggle's expected behaviour" , async ({page, accessibility}) => {
     |         ^ Error: browserType.launch: Target page, context or browser has been closed
  17 |         await accessibility.checkIncreaseTextFunctionality();
  18 | })
  19 | })
  20 |
```